VITE_API_URL=http://localhost:8000
# Google sign-in: OAuth client ID (Web app) from Google Cloud Console.
# Authorized JavaScript origins must include the frontend URL(s).
VITE_GOOGLE_CLIENT_ID=
# EmailJS (OTP delivery). Set these, then rotate the previously hardcoded
# demo key in the EmailJS dashboard and remove the in-code fallbacks.
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=