// Payoneer lives inside the unified Income hub (PayPalDashboard) —
// this wrapper keeps the "payoneer-dashboard" route working so all
// existing links (Payoneer success screen, callbacks) land on the hub.
import Hub from "./PayPalDashboard.jsx";

export default Hub;
